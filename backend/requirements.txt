fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# bcrypt>=4.1 enforces a 72-byte limit and raises ValueError during passlib 1.7.4's
# backend self-check (detect_wrap_bug), breaking startup. Pin to 4.0.x to keep the
# legacy silent-truncation behavior passlib expects.
bcrypt<4.1
alembic>=1.13.0
python-multipart>=0.0.12
Pillow>=10.0.0
httpx>=0.27.0
colorama>=0.4.6
pytest>=8.0.0
pytest-asyncio>=0.24.0
