# 项目协作规则

## 一、核心开发原则
1. 不需要向后兼容。采用当前（2026 年）成熟、主流的技术方案，优先保证架构合理性和长期可维护性。
2. 禁止补丁式开发。必须追查问题根因，从根本上解决问题，不允许通过叠加特殊判断、临时兜底或兼容层掩盖设计缺陷。
3. 宁可合理调整架构，也不保留不必要的历史包袱。重构时必须同步更新所有受影响的调用方。
4. 保持代码简洁、职责清晰，避免过度设计、重复实现和不必要的抽象。

## 二、修改前检查
每次修改代码前，必须：
1. 阅读相关代码，理解现有架构、模块职责及完整调用链。
2. 搜索项目中已有的相同或相似实现，优先复用现有公共函数、组件和服务。
3. 对 Bug 追查根因，评估是否存在同类问题，避免只修复表面症状。
4. 评估修改对其他模块、接口、数据结构和业务流程的影响。
5. 如果发现现有架构不合理，优先考虑系统性解决方案，而不是绕过架构增加特殊逻辑。

## 三、代码复用与架构规范
1. 遵循单一职责原则，保持模块边界清晰，避免不合理的跨层调用和循环依赖。
2. 发现重复或高度相似的逻辑时，评估抽取公共函数、Hook、Service 或组件。
3. 相同的业务规则应有统一实现，避免多处独立维护。
4. 禁止创建功能重复的新模块，禁止为了修复局部问题而引入不必要的全局复杂性。
5. 不为了减少代码量而过度抽象。只有存在合理的职责边界或真实复用价值时才进行抽取。
6. 修改核心架构前，先说明调整方案及影响范围，等待用户确认。

## 四、Bug 修复规范
1. 必须优先修复根本原因，而非仅修复触发问题的单一路径。
2. 检查同类问题是否存在于其他模块。
3. 不允许通过吞噬异常、无依据的重试、硬编码或增加特殊分支掩盖问题。
4. 不需要保留旧实现的兼容逻辑，但必须同步迁移所有受影响的内部调用。
5. 对已确认的 Bug 补充适当的回归测试，防止问题再次出现。

## 五、修改后循环审查
每次修改完成后，必须进行以下循环：
1. 审查本次所有代码差异及受影响的调用链。
2. 检查逻辑错误、边界条件、异步并发、异常处理、资源释放及潜在安全问题。
3. 检查是否引入重复代码、临时补丁、不必要的兼容层或架构退化。
4. 运行相关测试、类型检查和静态检查。
5. 发现问题后修复，并再次执行相关审查和测试。
持续循环，直到没有已发现且可复现的问题，且相关检查通过。
若连续修复仍无法解决问题，或发现超出任务范围的架构缺陷，应停止盲目修改，报告原因和建议方案。
禁止在未经验证的情况下宣称不存在 Bug。

## 六、修改范围与权限
1. 允许为根因修复而合理调整相关架构，但禁止无关重构。
2. 大规模重构、数据库结构变更、核心技术方案调整及破坏性操作必须事先获得用户确认。
3. 禁止未经授权修改或删除生产数据、执行生产部署。
4. commit、push、merge 必须经过用户明确确认。
5. 完成工作后只报告结果，不主动提交或推送代码。

## 七、最终交付报告
每次完成开发任务后，简要报告：
1. 本次修改的内容及涉及模块。
2. 发现并修复的 Bug 清单及其根本原因。
3. 重构、公共函数抽取及重复代码清理情况。
4. 执行的测试、检查结果及未覆盖的部分。
5. 是否存在遗留问题、潜在风险或需要用户确认的事项。
报告必须区分已验证的事实与尚未验证的推测。